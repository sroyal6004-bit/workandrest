rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 연결 코드(10자 이상)를 아는 기기만 해당 문서를 읽고 쓸 수 있음. 목록 조회는 금지.
    match /spaces/{spaceId} {
      allow get, write: if spaceId.size() >= 10;
      allow list: if false;
    }
  }
}
